version: 1
frontend:
  phases:
    preBuild:
      commands:
    build:
      commands:
        - gem install aws-sdk-secretsmanager
        - ruby secret-test.rb
  artifacts:
    baseDirectory: /build/site
    files:
      - '**/*'
  cache:
    paths: []